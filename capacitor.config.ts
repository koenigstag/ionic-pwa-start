import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-pwa-start',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
